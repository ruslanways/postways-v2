# Postways v2 environment variables (example)
#
# Usage:
# - Copy this file to `config/.env`
# - Replace placeholder values (especially secrets/passwords)
#
# Notes:
# - Do NOT commit `config/.env` (real secrets) to git.
# - This project loads env vars from `config/.env` (see `config/settings.py`).
#
#
# =============================================================================
# Django Core
# =============================================================================
#
# Environment mode: "development" or "production"
# - development: DEBUG=True allowed, local filesystem storage, permissive CORS
# - production: Security hardening, S3 media storage, ManifestStaticFilesStorage
DJANGO_ENV=development
#
# Django secret key used for cryptographic signing.
# - Development: any random string works
# - Production: MUST be long and unpredictable (keep it secret!)
DJANGO_SECRET_KEY=change-me-in-production-use-a-long-random-string
#
# DEBUG is automatically derived from DJANGO_ENV:
# - development → DEBUG=True
# - production → DEBUG=False
#
#
# =============================================================================
# Hosts & Security (Production only)
# =============================================================================
#
# Required when DJANGO_ENV=production
# Comma-separated list of allowed hosts
# ALLOWED_HOSTS=postways.com,www.postways.com
#
# Comma-separated list of trusted origins for CSRF
# CSRF_TRUSTED_ORIGINS=https://postways.com,https://www.postways.com
#
#
# =============================================================================
# Database (PostgreSQL)
# =============================================================================
#
# Connection string parsed by `django-environ` via `env.db()` in `config/settings.py`.
#
# Option A (recommended for Docker Compose):
# - Host is the Compose service name `db`
# - Port is the container port 5432
DATABASE_URL=postgresql://admin:change-me@db:5432/postways
#
# Option B (when running Django on your HOST, with Postgres still in Compose):
# - `docker/docker-compose.yml` exposes Postgres on 127.0.0.1:5434
# DATABASE_URL=postgresql://admin:change-me@127.0.0.1:5434/postways
#
#
# -----------------------------
# Postgres Container (docker-compose `db` service)
# -----------------------------
#
# These variables configure the Postgres container on first start.
# Keep them in sync with `DATABASE_URL` above.
POSTGRES_DB=postways
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me
#
#
# =============================================================================
# Redis
# =============================================================================
#
# Redis connection settings used by Django (Channels/WebSocket, Celery).
#
# Option A (recommended for Docker Compose):
# - Host is the Compose service name `redis`
# - Port is the container port 6379
REDIS_HOST=redis
REDIS_PORT=6379
#
# Option B (when running Django on your HOST, with Redis on localhost):
# REDIS_HOST=localhost
# REDIS_PORT=6379
#
# Note: Defaults to `redis:6379` if not set (Docker Compose service name).
#
#
# =============================================================================
# Email Configuration
# =============================================================================
#
# SMTP settings for sending emails (e.g., token recovery, weekly reports).
#
# For Gmail:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Other (Custom name)" → name it "Postways"
#    - Copy the 16-character password (no spaces)
# 3. Use these settings:
#    EMAIL_HOST=smtp.gmail.com
#    EMAIL_PORT=587
#    EMAIL_HOST_USER=your-email@gmail.com
#    EMAIL_HOST_PASSWORD=your-16-char-app-password  # NOT your regular Gmail password!
#    EMAIL_USE_TLS=True
#    DEFAULT_FROM_EMAIL=your-email@gmail.com
#
# For other SMTP providers, adjust EMAIL_HOST and EMAIL_PORT accordingly.
# Common ports: 587 (TLS), 465 (SSL)
# Note: TLS and SSL are mutually exclusive. For port 465, set EMAIL_USE_TLS=False and EMAIL_USE_SSL=True
#
# In DEBUG mode, if EMAIL_HOST is not set, emails will print to the console instead of sending.
#
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=change-me-to-your-gmail-app-password
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_TIMEOUT=30
# DEFAULT_FROM_EMAIL=your-email@gmail.com
# WEEKLY_RECIPIENTS=recipient1@example.com,recipient2@example.com
#
#
# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================
#
# Allowed origins for cross-origin requests to the API.
# Only used when DEBUG=False. In DEBUG mode, all origins are allowed.
#
# Comma-separated list of origins (scheme + domain + port):
# CORS_ALLOWED_ORIGINS=https://example.com,https://app.example.com
#
# Leave empty or omit to disallow all cross-origin requests in production.
# CORS_ALLOWED_ORIGINS=
#
#
# =============================================================================
# AWS S3 (Production only - Media Storage)
# =============================================================================
#
# Required when DJANGO_ENV=production
# Used for storing user-uploaded media files (images, etc.)
#
# 1. Create an S3 bucket with public read access
# 2. Create an IAM user with S3 write permissions
# 3. Generate access keys for the IAM user
#
# AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
# AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=eu-central-1
