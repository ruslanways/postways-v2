# Postways v2 Production Environment Variables
#
# Copy this file to `config/.env` on your production server.
# Replace ALL placeholder values before deploying.
#
# =============================================================================
# Django Core
# =============================================================================
DJANGO_ENV=production

# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=CHANGE-THIS-TO-A-LONG-RANDOM-STRING

# =============================================================================
# Hosts & Security
# =============================================================================
# Your domain(s), comma-separated
ALLOWED_HOSTS=postways.example.com,www.postways.example.com

# Include https:// prefix for CSRF origins
CSRF_TRUSTED_ORIGINS=https://postways.example.com,https://www.postways.example.com

# =============================================================================
# Database (PostgreSQL)
# =============================================================================
# Use strong password in production
DATABASE_URL=postgresql://postways_user:STRONG-PASSWORD-HERE@db:5432/postways_db

# Postgres container initialization (keep in sync with DATABASE_URL)
POSTGRES_DB=postways_db
POSTGRES_USER=postways_user
POSTGRES_PASSWORD=STRONG-PASSWORD-HERE

# =============================================================================
# Redis
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379

# =============================================================================
# Email Configuration
# =============================================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_TIMEOUT=30
DEFAULT_FROM_EMAIL=noreply@postways.example.com
WEEKLY_RECIPIENTS=admin@postways.example.com

# =============================================================================
# CORS (if you have a separate frontend)
# =============================================================================
# CORS_ALLOWED_ORIGINS=https://app.postways.example.com

# =============================================================================
# AWS S3 (Media Storage)
# =============================================================================
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AWS_STORAGE_BUCKET_NAME=postways-media
AWS_S3_REGION_NAME=eu-central-1
# Optional: CDN domain for media (e.g., CloudFront)
# CDN_DOMAIN=media.postways.example.com
